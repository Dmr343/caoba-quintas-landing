# 99-archivo

Material viejo o inactivo que se conserva pero ya no se usa. No se publica ni se mantiene.

Contenido:
- `index-dev.html` — versión vieja del landing (título "Lotes en Costa Rica", superada por la de producción "Bagaces, Guanacaste").
- `tweaks-panel.jsx` — herramienta dev de ajustes (ya no se carga; solo quedaba un comentario en `index.html`).
- `Inventario24Feb2026.jpeg` — foto de inventario suelta, sin referencias.
- `Videos/` — 23 fotos fuente del dron (gitignored, pesadas).
- `dist/` — build viejo regenerable (gitignored).
